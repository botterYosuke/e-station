# Tachibana secret scan patterns (F-L1 / T7)
# Each non-comment, non-blank line is a grep -E regex.
# Matches in files listed in secret_scan_allowlist.txt are suppressed.

# 立花本番ホスト（demo 含む全 URL を検出）
kabuka\.e-shiten\.jp

# 認証フィールド名（生値が IPC/ログに露出しないことを保証）
\bsUserId\b\s*[:=]
\bsPassword\b\s*[:=]
\bsSecondPassword\b\s*[:=]

# BASE_URL_PROD 定数（single-source ルール F-L1: tachibana_url.py 以外での定義は禁止）
BASE_URL_PROD\s*=
